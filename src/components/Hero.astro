<section class="relative min-h-screen flex items-center justify-center text-center overflow-hidden">

  <!-- Fondo -->
  <div class="absolute inset-0">
    <img 
      src="https://images.unsplash.com/photo-1559339352-11d035aa65de?auto=format&fit=crop&w=1950&q=80"
      class="w-full h-full object-cover scale-110"
      alt="Bocuze Bistro"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-black/85 via-black/70 to-black/90"></div>
  </div>

  <!-- Contenido -->
  <div class="relative z-10 px-6">

    <!-- Badge -->
    <span class="inline-block mb-8 px-6 py-2 border border-amber-600/50 text-amber-500 text-xs tracking-[0.35em] uppercase font-semibold rounded-full bg-black/40 backdrop-blur-sm">
      Zibatá, Querétaro
    </span>

    <!-- Título gigante -->
    <h1 class="text-6xl md:text-8xl lg:text-9xl font-serif text-white leading-[1.05] tracking-tight">
      Alta Cocina
      <br/>
      <span class="italic text-zinc-300 font-light">
        Bajo las Estrellas
      </span>
    </h1>

    <!-- Descripción más grande -->
    <p class="mt-10 text-xl md:text-2xl text-zinc-400 font-light max-w-3xl mx-auto leading-relaxed">
      Una experiencia gastronómica que combina la técnica francesa con ingredientes locales en un ambiente nocturno inigualable.
    </p>

    <!-- Botones -->
    <div class="mt-14 flex flex-col md:flex-row items-center justify-center gap-8">
      <button class="bg-amber-700 hover:bg-amber-600 text-white px-12 py-5 uppercase tracking-[0.25em] text-sm font-bold transition-all duration-300 hover:scale-105 shadow-2xl">
        Reservar Mesa
      </button>

      <a href="#menu" class="uppercase tracking-[0.25em] text-sm font-bold text-white hover:text-amber-500 transition-colors flex items-center gap-2">
        Explorar Menú →
      </a>
    </div>

  </div>
</section>
